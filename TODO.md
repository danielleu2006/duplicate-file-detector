# TODO — Duplicate File Detector

---

## 当前优先级最高

- [ ] 在真实大文件夹上测试性能（1 万+ 文件）
- [ ] 确认 macOS / Linux GUI 正常运行

## 接下来可以做

- [ ] 感知哈希（pHash）支持 — 检测视觉相似的图片（即使字节不同）
- [ ] 硬链接去重 — 用硬链接替代重复文件（节省空间但保留路径）
- [ ] 文件预览面板 — 在 GUI 中预览选中的图片/文本文件
- [ ] 扫描进度文件计数 — Phase 1 也显示进度（当前只显示哈希进度）
- [ ] 递归深度限制 — `--max-depth` 选项
- [ ] 排除模式 — `--exclude` 支持通配符（如 `*.tmp`, `*.log`）
- [ ] 多路径扫描 — 同时扫描多个文件夹
- [ ] 增量扫描 — 记住已哈希的文件，仅扫描新增/修改的文件
- [ ] GUI 暗色主题 — 跟随 Daniel 其他项目的深色日志风格
- [ ] PyInstaller 打包 — 独立可执行文件分发

## 以后再做

- [ ] 拖放文件夹到 GUI 窗口
- [ ] 系统托盘集成
- [ ] 右键菜单集成（Windows 资源管理器）
- [ ] 扫描结果对比（两次扫描之间新增/消失的重复）
- [ ] 网络驱动器优化（缓存哈希结果）

## 已完成

- [x] CLI 扫描引擎（3 层策略：大小 → SHA-256 → 分组）
- [x] CLI 接口（argparse，`--json` / `--delete` / `--stage` / `--skip` 等）
- [x] Tkinter GUI（扫描设置、结果树视图、复选框、操作按钮）
- [x] 删除操作（GUI + CLI，确认提示）
- [x] 移动到暂存目录（保留目录结构）
- [x] JSON 导出
- [x] 进度条 + 取消支持
- [x] 设置持久化（窗口位置、路径、选项）
- [x] 默认跳过目录列表（.git, node_modules 等 15 个）
- [x] `--delete` / `--stage` 互斥校验
- [x] 代码审查修复（死代码删除、线程安全、stderr 修正、导入位置修正）
- [x] GUI 启动冒烟测试 + 程序化扫描路径测试
- [x] 完整用户手册（USER_MANUAL.md，11 章节）
- [x] 项目笔记（PROJECT_NOTES.md）
- [x] TODO 追踪（本文件）
- [x] Bug 修复：删除后重新扫描若无重复，Results 面板不刷新（_on_scan_complete 空结果分支未清除旧 tree 数据）
